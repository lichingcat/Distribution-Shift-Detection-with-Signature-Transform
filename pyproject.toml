[project]
name = "changepoint-detection"
version = "0.0.1"
authors = [
]
description = "Changepoint detection using signature transform"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
]
dependencies = [
  "pandas",
  "datasets",
  "matplotlib"
]

[build-system]
requires = ["setuptools>=61.0", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]